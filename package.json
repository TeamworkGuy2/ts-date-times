{
  "name": "ts-date-times",
  "version": "0.4.0",
  "description": "Manage dates, timestamps, and .NET DateTime JSON in TypeScript",
  "dependencies": {
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "~3.0.0",
    "typescript": "~1.8.9"
  },
  "scripts": {
    "test": "mocha -u tdd --recursive"
  }
}
